# Copy to .env beside the ingress compose file. Never commit .env.
INGRESS_NETWORK=agent-ingress
# Pin a release or digest after validating it for your platform.
CADDY_IMAGE=ghcr.io/skb50bd/caddy:latest
# Required only by app stacks that select compose.tls-cloudflare.yaml.
CLOUDFLARE_API_TOKEN=
